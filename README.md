# TDD Calculator

A simple calculator written in Java using TDD principles.

## Running tests

```bash
./gradlew test
```

## Generating a test report

Run the tests — the HTML report is generated automatically at:

```
app/build/reports/tests/test/index.html
```

Open the file in a browser to view results.
